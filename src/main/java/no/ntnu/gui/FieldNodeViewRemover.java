package no.ntnu.gui;

/**
 * A remover of field node views.
 */
public interface FieldNodeViewRemover {
    /**
     * Removes the field node view with the given address.
     *
     * @param fieldNodeAddress the address of the field node view
     */
    void removeFieldNodeView(int fieldNodeAddress);
}
